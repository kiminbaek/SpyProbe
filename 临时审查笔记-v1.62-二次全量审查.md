# SpyProbe 二次全量源码审查笔记（v1.62.0 基线）

> 创建：2026-08-12 ｜ 范围：全部自有源码（55 文件 21,434 行 Java/Kotlin + native_hook.cpp 1,372 行 + http2_parser.cpp 433 行）
> 用户要求：「全面审核所有源代码 / 临时审查笔记实时更新 / bug+优化+稳定 / 三大核心（抓包齐全正确稳、探测准稳、日志完善准确加密解密全结构化有分析页）/ **特别强调：抓包探测信息是否显示在日志界面、有无遗漏**」

## 审查重点（本轮新增）
- 🔴 **数据链路完整性审计**：每个捕获源（NetProbe/NativeProbe/各 Probe）→ 日志行 / EventStore / HttpStore → UI（LogsScreen 卡片 / HttpDetailScreen 详情 / AnalysisScreen）→ **是否有信息抓到但没显示**
- 三大核心功能把关：抓包（齐全/正确/稳）、探测（准/稳）、日志（完善/准确/加密解密/结构化/分析页）

## 审查文件清单
- [ ] native_hook.cpp (1372) — 抓包核心 native 层
- [ ] http2_parser.cpp (433) — H2 帧解析
- [ ] NetProbe.java (1305) — Java 层 OkHttp 抓包核心
- [ ] NativeProbe.java (497) — native 回调 Java 端
- [ ] TlsHttpParser.java (504) — TLS 明文→结构化
- [ ] HttpEntry.java (492) — 结构化数据模型
- [ ] LogStore.java (305) / HttpStore.java / EventStore 相关 — 存储层
- [ ] LogPersister.java (570) — 落盘
- [ ] SpyHomeServer.java (409) / SpyServer.java (633) — 控制面
- [ ] HomeLogReader.kt (263) — 历史读取
- [ ] LogsScreen.kt (1511) — 日志主界面
- [ ] HttpDetailScreen.kt (1028) — 详情页
- [ ] EventCard.kt (549) — 卡片
- [ ] CaptureScreen.kt (923) / SettingsScreen.kt (1012) — 设置
- [ ] AnalysisScreen.kt (383) / SpyViewModel.kt (595) / Api.kt (709) — 数据流
- [ ] 各 Probe：MethodProbe(768) / CryptoProbe(669) / AntiDetectProbe(597) / EnvProbe(465) / SQLiteProbe(449) / DexKitProbe(304) / CrashCatcher / JsonProbe / UrlProbe / LogCatProbe / ClassLoadProbe / ActivityProbe / PrefsProbe / KeystoreProbe / WebViewProbe / SensorProbe
- [ ] ModuleMain.java (264) / Config.java (628) / PcapWriter.java (457) / Updater.kt (312) / OkHttpReplay / PcapStore / ShareLogUtil / TokenStore / SpyEvent / MainActivity

## 进度记录
（审查中实时更新）

## 发现的问题
（审查中实时更新）

## 发现的问题（实时更新）

### P1（重要）
1. **MethodProbe 规则引擎执行记录未结构化**：`[RULE:return/block/param/static/recordParams/recordBoth]` 全是纯文本 LogStore.log，无 [EVT#] 无 EventStore → 用户自定义规则命中（改返回值/拦执行/改参数）是逆向核心操作，但日志页只显示文本行、无卡片、无详情页（MethodProbe.java:412-457）
2. **NetProbe SSL 绕过拦截未结构化**：`[SSL] setCertificatePinner intercepted (bypass)` / `xutils trust-all` / `Cronet pinning bypass`（NetProbe.java:357/374/383/442/454）纯文本无 [EVT#] → SSL pinning 绕过是抓包核心能力，成功与否应卡片化展示
3. **OkHttp 混淆兜底未结构化**：`[OkHttp-混淆] <<< code url`（NetProbe.java:1061）纯文本无 [REQ#] 无 HttpEntry → execute() 兜底路径抓到请求但 UI 无卡片（REALCALL/HUC/CRONET 都已修，这个漏了）

### P2（优化）
1. **tlsHttpFeed 未在 isUnknown() 后短路**：注释说"unknown 后不再喂解析器"但代码没实现，unknown 连接数据仍进 feed（浪费 CPU，无功能缺失）→ 加 `if (p.isUnknown()) return;`
2. **H2 DATA 每块刷一行**：onH2DataChunk 每 DATA 帧都打 `[H2 DATA ...]` 文本行（v1.52.1 删了 TLS 摘要行但 H2 数据行还在）→ 视频分片场景 H2 大流量可能刷屏；body 已进 HttpEntry 详情页，文本行可降级

## 数据链路审计进度
- [x] EventStore/SpyEvent：结构 OK，UI 卡片 type 全覆盖（含 else 兜底）
- [x] 各 Probe 写入配对：Activity/AntiDetect/ClassLoad/Crypto/Env/Json/Keystore/LogCat/Method(规则引擎除外)/Prefs/SQLite/Url 全部 [EVT#]+EventStore 双写 ✅
- [x] NetProbe：NET 事件 ✅ / REQ# 链路 ✅ / REALCALL ✅ / HUC ✅ / CRONET ✅ / **SSL 绕过 ❌** / **混淆兜底 ❌**
- [x] NativeProbe：TLS 结构化 ✅ / H2 结构化 ✅ / UNKNOWN 标签 ✅ / KeyLog ✅ / **H2 DATA 刷屏 ⚠️**
- [ ] LogsScreen 解析链路（EVT#/REQ# → 卡片）
- [ ] SpyHomeServer / HomeEventStore / HomeHttpStore / LogPersister
- [ ] SpyViewModel / Api / AnalysisScreen
- [ ] PcapWriter / OkHttpReplay / Updater
- [ ] Config 开关与 Probe 对应（探测是否漏开/漏接）
- [ ] CrashCatcher / DebugLog / ModuleMain / HookSafe

### P1（重要）- 续
4. **🔴 native 未 hook SSL_write_ex/SSL_read_ex**：BoringSSL/OpenSSL 1.1.1+ 提供 ex 变体，直接调 ex API 的库（部分 Flutter/WebSocket/自定义 TLS 库）会绕过 SSL_write/SSL_read hook → **漏抓明文**。Android libssl.so 导出 SSL_write_ex（API 29+），应补 4 库（libssl/libconscrypt_jni/libttboringssl/libflutter）的 ex 变体 hook（native_hook.cpp g_ssl_hooks 数组 + hook 初始化）
5. **HomeEventStore.readDay 无条数上限**：HomeHttpStore.readDay 有 5000 上限（v1.62 P2-15），事件文件全量读内存（每日所有 SQL/JSON/CRYPTO/...），历史页大量数据时 OOM/卡顿风险（HomeEventStore.java:90）
6. **历史回溯 firstOrNull 全量读**：LogsScreen.kt:585/613 readDay 全量加载后 firstOrNull 找 id；HTTP readDay 默认 5000 条上限会截断，早于 5000 条的历史卡片回溯失败 → 详情页退化纯文本（虽不崩，但卡片丢失）

### P2（优化）- 续
3. **HTTP/1.1 chunked 响应体后续 chunk 内容不追加**：TlsHttpParser.feedResponseBody 只计数 respBodyAcc，chunked 后续段 body 内容不进 e.respBody（详情页 body 只有头同段部分）；而 H2 的 onH2DataChunk 会追加 body → 不一致。防 OOM 有意设计，但可加"chunked 段内可读文本累积至 bodyMax"（TlsHttpParser.java:415-430）
4. **chunked 结束标记跨段漏检**：findChunkedEnd 只查当前段内 "0\r\n\r\n"，若标记跨段（前段尾 "0\r\n" + 后段头 "\r\n"）→ 该请求 WAIT_RESP_BODY 直到连接关闭/新请求才 complete（respBodyBytes 偏大，耗时拉长）。真实场景低概率（P3 可并入 P2-3 修）

### 数据链路审计进度（续）
- [x] TlsHttpParser：解析完整 ✅（1xx 跳过/body 分段收集/chunked/close 兜底）
- [x] LogStore 推送链路：队列满丢最旧（保最新，可接受）✅
- [x] LogPersister：目标进程空转保护 ✅ / 主进程 init ✅
- [x] MainActivity：全部 Store 初始化 ✅
- [x] SpyViewModel：9900/9901 双轮询 + 环形缺口检测 ✅
- [ ] NetProbe OkHttp 主 hook 链路
- [ ] HttpEntry raw 方法
- [ ] SettingsScreen/ProbeScreen/HooksScreen
- [ ] PcapWriter/PcapStore/Updater/OkHttpReplay/TokenStore/HomeLogReader
- [ ] DebugLog/CrashCatcher/HookSafe/StackUtil/JsonProbe/ActivityProbe/ClassLoadProbe/KeystoreProbe/LogCatProbe/DexKitProbe/CryptoProbe 后半

## 第一轮审查结论（2026-08-12 完成全量扫描）

### ✅ 确认无误的核心链路
- 数据链路：所有 Probe 捕获 → [EVT#]/[REQ#] 日志行 + EventStore/HttpStore 双写 → push 主进程 → 落盘（http_entries/event_entries/spyprobe_logs JSONL）→ UI 卡片化（实时+历史）✅
- EventCard payloadGroups 覆盖全部 SpyEvent type（SQL/JSON/CRYPTO/NET/URL/CLIP/LOG/ACT/DETECT/PREFS/CLASS/METHOD/CERT + else 兜底）✅
- TlsHttpParser：1xx 跳过 / body 分段收集 / chunked / close 兜底 / 独立 httpId ✅
- native：SSL_write/read/free + NativeCrypto 变体 + send/recv + H2 HPACK 解码 + keylog + TLS meta + UNKNOWN 标签 ✅
- SSL 绕过（pinningHit）：已结构化 DETECT 事件（Pinning#N + [EVT#]），12+ 覆盖点 ✅
- 日志页分类过滤（ALL/NET/MTH/RULE/CRYPTO/CLS/SYS）+ 关键词过滤 ✅
- 自家日志 vs 目标数据隔离（DebugLog logNoMirror 不污染日志页）✅

### 🔴 P1（应修，共 3）
| # | 问题 | 位置 | 影响 |
|:-:|:-----|:-----|:-----|
| P1-1 | **规则引擎 RULE:* 未结构化**（return/block/param/static/recordParams/recordBoth 纯文本，无 [EVT#]/EventStore） | MethodProbe.java:412-457 | 用户自定义规则命中（改返回值/拦执行）是逆向核心操作，只有文本行、无卡片、无详情页 |
| P1-2 | **OkHttp 混淆 execute() 兜底未结构化**（[OkHttp-混淆] <<< 纯文本，无 [REQ#]/HttpEntry） | NetProbe.java:1061 | 正常 hook 失败走 execute() 兜底时，抓到请求但 UI 无 REQ# 卡片（REALCALL/HUC/CRONET 均已修，此处漏） |
| P1-3 | **native 未 hook SSL_write_ex/SSL_read_ex** | native_hook.cpp hook 初始化 | 直接调 ex API 的库（BoringSSL API 29+ / OpenSSL 1.1.1+ 场景）绕过 SSL_write hook → **漏抓 TLS 明文** |

### 🟡 P2（建议修，共 7）
| # | 问题 | 位置 | 影响 |
|:-:|:-----|:-----|:-----|
| P2-1 | tlsHttpFeed 未在 isUnknown() 后短路 feed（注释声称已停但代码没实现） | NativeProbe.java:100 | unknown 连接数据仍喂解析器，浪费 CPU（无功能缺失） |
| P2-2 | H2 DATA 每块刷屏一行（onH2DataChunk 每条 DATA 帧都打 [H2 DATA] 文本行） | NativeProbe.java:413 | 视频分片 H2 大流量可能刷屏（v1.52.1 删了 TLS 摘要行，H2 漏了）；body 已进 HttpEntry，文本行可降级 |
| P2-3 | HTTP/1.1 chunked 响应体后续 chunk 内容不追加（只计数） | TlsHttpParser.java:415-430 | 详情页 chunked body 只有头同段部分；H2 已追加形成对比 |
| P2-4 | chunked 结束标记跨段漏检（findChunkedEnd 只查当前段） | TlsHttpParser.java:440 | 标记跨段时请求延迟到连接关闭才 complete（低概率） |
| P2-5 | HomeEventStore.readDay 无条数上限（HomeHttpStore 有 5000） | HomeEventStore.java:90 | 历史页事件文件全量读内存，大量数据 OOM/卡顿风险 |
| P2-6 | 历史回溯 firstOrNull 全量读 + HTTP readDay 5000 截断 | LogsScreen.kt:585/613 | 早于 5000 条的历史卡片回溯失败 → 详情页退化纯文本 |
| P2-7 | AnalysisScreen 只聚合 HTTP/SQL/CRYPTO/NET，缺 DETECT/URL/PREFS/CLASS/METHOD/LOG/ACT/CERT | AnalysisScreen.kt | 分析页不完整（用户要"完整详细的分析解剖页面"） |

### ⚪ P3（可不改，记录）
- OkHttpReplay [Replay#N] 纯文本（用户主动操作，非目标数据）
- NetProbe [SSL] 前缀行冗余（pinningHit 已结构化 DETECT）

## 待用户确认修复范围
